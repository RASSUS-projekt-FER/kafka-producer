# Ovaj dokument opisuje arhitekturu klastera (teme i slično)
# podaci za čitanje su navedeni kao csv podaci
# komentari započinju s '#'

# Korištene metrike:
# -id uređaja
# -naziv uređaja
# -CPU usage
# -RAM usage
# -no of active connections
# -TCP received
# -TCP sent
# -UDP sent
# -UDP received
# -vrijeme zadnjeg paljenja

# format konfiguracije čvora:
# id, naziv, avg cpu, cpu devijacija, avg ram, ram devijacija, avg tcp send, avg tcp rec, avg udp send, avg udp rec

1,plain-pc,35,10,35,15,10,10,10,10